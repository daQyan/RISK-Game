package ece651.RISC;

public class Status {
    public enum playerStatus{
        WIN,
        PLAYING,
        LOSE
    }
    public enum actionStatus{
        MOVE,
        ATTACK
    }
    public enum gameStatus{
        PLAYING,
        FINISHED
    }
}
